#include <iostream>

using namespace std;

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);
    
    long long a;
    cin >> a;
    
    cout << a * a * a << '\n' << 3;
    
    return 0;
}